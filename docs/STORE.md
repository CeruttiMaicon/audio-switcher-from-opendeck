# Materiais para listagem / loja

Para páginas de projeto (GitHub, OpenDeck, etc.), convém ter:

1. **Captura do OpenDeck** — tecla com o plugin + Property Inspector aberto (lista de dispositivos visível).  
   Guarde como `screenshot-opendeck.png` nesta pasta e referencie no README principal.

2. **GIF curto (opcional)** — alternar entre duas saídas e o ícone da tecla a mudar de estado.

O README na raiz já usa o ícone do plugin como pré-visualização mínima até existir captura real.
